# Architecture Review — Delta #6 (ADR-0005 Input Handling)

> Generated by `/architecture-review ADR-0005` on 2026-04-30 (lean delta-mode review focused on Proposed → Accepted escalation of newly-authored ADR-0005). Disambiguation suffix `b` distinguishes from `architecture-review-2026-04-30.md` (ADR-0007 delta) earlier same date.

| Field | Value |
|---|---|
| Mode | delta (single-ADR Proposed → Accepted escalation) |
| Date | 2026-04-30 |
| Engine | Godot 4.6 (pinned 2026-04-16; reference docs verified 2026-02-12) |
| ADR under review | ADR-0005 Input Handling — InputRouter Autoload + 7-State FSM (MVP scope) |
| GDDs Reviewed | design/gdd/input-handling.md (Designed 2026-04-16; Status header refreshed this pass) |
| ADRs Reviewed | 9 Accepted (ADR-0001..0004, 0006..0009, 0012) for cross-conflict scan + ADR-0005 (Proposed → Accepted this pass) |
| Verdict | **PASS** (with 6 pre-Acceptance precision-gap corrections applied same-patch; 2 advisories carried as Implementation Notes D + E; 1 post-Acceptance ADR-0001 amendment queued) |
| Pattern invocation | 6th /architecture-review delta in pattern-stable invocation track (avg ~2 wording corrections / delta — this delta = 6 corrections, above average for first HIGH-engine-risk ADR + longer ADR surface) |

---

## Phase 1 — Loaded Inputs

**Documents read in full**:
- `docs/architecture/ADR-0005-input-handling.md` (484 lines pre-corrections)
- `design/gdd/input-handling.md` (835 lines)
- `docs/engine-reference/godot/{VERSION,breaking-changes,deprecated-apis,current-best-practices}.md`
- `docs/engine-reference/godot/modules/{input,ui}.md`
- `docs/architecture/{ADR-0001-gamebus-autoload,ADR-0002-scene-manager,ADR-0004-map-grid-data-model}.md` (specifically the Input cross-ref regions: ADR-0001 lines 167-174 + 252 + 329-335 + 365-376; ADR-0002 lines 41-44 + 182-310; ADR-0004 get_tile public query)
- `docs/architecture/tr-registry.yaml` (v7 → v8 this pass)
- `docs/architecture/architecture-traceability.md` (v0.6 → v0.7 this pass)
- `docs/architecture/architecture.md` (v0.4)
- `docs/registry/architecture.yaml` (v2 → v3 this pass; lines 193-204 + 408-425 + 646-665 + 820-860 input-domain entries)
- `docs/architecture/control-manifest.md` (no change this pass)
- `.claude/docs/technical-preferences.md`

---

## Phase 2 — Technical Requirements Extracted from GDD

design/gdd/input-handling.md authored 2026-04-16 with 5 Core Rules CR-1..CR-5 (CR-1 22-action vocabulary + CR-1a no-hover-only + CR-1b external bindings + CR-1c grid_hover PC-only; CR-2 last-device-wins + CR-2c state preservation + CR-2e emulate_mouse_from_touch=false; CR-3 two-beat confirmation + CR-3a-c sub-rules; CR-4a-h Touch protocol — TPP / 44px / Magnifier / Selection / 80ms / Pan-vs-tap / Two-finger / Action Panel; CR-5a-f Per-unit undo); 22-action vocabulary (10 grid + 4 camera + 5 menu + 3 meta); 7-state FSM (S0..S6); 3 formulas F-1..F-3; 10 Edge Cases EC-1..EC-10; 8 Tuning Knobs TK-1..TK-8; 18 Acceptance Criteria AC-1..AC-18; 5 Open Questions OQ-1..OQ-5 (3 partially resolved this ADR — OQ-1/OQ-2/OQ-3; OQ-4/OQ-5 deferred to Battle HUD + playtest).

The traceability index pre-state listed 11 candidate TRs under input-handling.md baseline. This pass collapses the candidate list and registers **16 net-new TR-input-handling-002..017** entries (the 11 candidates + 5 architecture-emergent TRs covering R-1..R-6 risks + cross-ADR consumer contracts).

---

## Phase 3 — Traceability Matrix

### Pre-state (registry v7 baseline)

| TR-ID | System | ADR | Status | Coverage |
|---|---|---|---|---|
| TR-input-handling-001 | input-handling | ADR-0001 | Accepted | ✅ Covered (signal exposure via GameBus, registered 2026-04-18 with ADR-0001 acceptance) |

### Post-state (registry v8 — 16 net-new TR-input-handling-002..017 registered this pass)

| TR-ID | ADR-0005 §Section | Coverage | Summary |
|---|---|---|---|
| TR-input-handling-002 | §1 Module form | ✅ Covered | Autoload Node load order 4; 5-precedent stateless-static rejected as Alternative 4 |
| TR-input-handling-003 | §4 + CR-1 | ✅ Covered | 22 StringName actions in ACTIONS_BY_CATEGORY const + JSON parity validation |
| TR-input-handling-004 | §4 + CR-1b | ✅ Covered | default_bindings.json external + InputMap.add_action/action_add_event API + set_binding mutation |
| TR-input-handling-005 | §3 + CR-2 | ✅ Covered | Most-recent-event-class rule; engine-validated dual-focus PASS Item 1 |
| TR-input-handling-006 | §5 | ✅ Covered | 7-state FSM inline match dispatch + ST-2 demotion + re-entrancy advisory |
| TR-input-handling-007 | §5 + CR-4 | ✅ Covered | Touch protocol — TPP / Magnifier / Pan-vs-tap / Two-finger gestures |
| TR-input-handling-008 | §7 + F-1 | ✅ Covered | camera_zoom_min = 44/64 = 0.6875 → 0.70 derivation via DisplayServer.screen_get_size |
| TR-input-handling-009 | §1 + §5 + CR-5 | ✅ Covered | Per-unit undo Dictionary[int, UndoEntry]; close conditions; tile-occupancy rejection |
| TR-input-handling-010 | §1 + ADR-0002 ratification | ✅ Covered | SceneManager block/unblock + set_process_input gates + INPUT_BLOCKED set_input_as_handled |
| TR-input-handling-011 | §6 | ✅ Covered | SDL3 gamepad → KEYBOARD_MOUSE pass-through (no GAMEPAD mode MVP) |
| TR-input-handling-012 | §7 | ✅ Covered | Android edge-to-edge / safe-area — 3-candidate API list with implementation-time verification |
| TR-input-handling-013 | §8 | ✅ Covered | _handle_event DI test seam + G-15 reset (with `_bindings.clear()` correction) |
| TR-input-handling-014 | §9 | ✅ Covered | 5 provisional contracts (Camera + Grid Battle + Battle HUD + Settings + Tutorial) |
| TR-input-handling-015 | §Verification + CR-2e | ✅ Covered | emulate_mouse_from_touch CI lint + 6 mandatory verification items |
| TR-input-handling-016 | §Performance + Validation | ✅ Covered | <0.05ms event dispatch / <10KB heap / Polish-deferred on-device measurement |
| TR-input-handling-017 | §4 + ADR-0001 reconciliation | ✅ Covered | Non-emitter for non-Input domains; Item 9 factual correction applied |

**Coverage**: 17/17 input-handling TRs covered (1 prior + 16 new). 0 gaps.

---

## Phase 4 — Cross-ADR Conflict Scan

**Result: 0 BLOCKING conflicts. 1 cross-doc precision-gap advisory carried.**

Pairwise scan against the 9 Accepted ADRs:

| Pair | Conflict found? | Notes |
|---|---|---|
| ADR-0005 ↔ ADR-0001 GameBus | ⚠️ 1 type-precision advisory (NON-blocking) | ADR-0001 line 168 declares `input_action_fired(action: String, ...)`; ADR-0005 + GDD line 379 + ACTIONS_BY_CATEGORY hot-path `&"..."` literals all use `StringName`. Auto-coerce at runtime in GDScript 4.x but breaks static-typing call-site precision. Defer to next ADR-0001 amendment (post-Acceptance carried advisory; queues with the line 372 `hero_database.get(unit_id)` advisory from ADR-0007 review). |
| ADR-0005 ↔ ADR-0001 batching obligation (line 252) | ✅ no conflict | Action-vocabulary abstraction satisfies "NOT from `_input(event)` for high-frequency input — use dedicated input batching in InputRouter" — InputRouter emits `input_action_fired` once per CONSUMED action (after state-machine dispatch), not per raw `InputEventMouseMotion` / `InputEventScreenDrag` frame. |
| ADR-0005 ↔ ADR-0001 non-emitter list | ✅ no conflict (post Item 9 correction) | The Item 9 factual correction now codified: InputRouter is NOT on ADR-0001 lines 370-377 non-emitter list (and never was). ADR-0001 line 329-335 explicitly registers InputRouter as Input-domain emitter — fully consistent. The ADR-0005 §Related Decisions wording previously claimed "MUST update to remove" — corrected this pass. |
| ADR-0005 ↔ ADR-0002 SceneManager `scene_transition_lifecycle` | ✅ no conflict | InputRouter consumes `ui_input_block_requested` / `ui_input_unblock_requested` per registry line 216 contract; ADR-0005 §1 + §Implementation Notes ratify the consumer side. |
| ADR-0005 ↔ ADR-0002 `overworld_pause_during_battle` | ✅ no conflict | SceneManager calls `InputRouter.set_process_input(false) + set_process_unhandled_input(false)` directly; ADR-0005 §1 confirms autoload Node receives BOTH callbacks (godot-specialist 2026-04-30 PASS Item 4). Recursive Control disable does not affect InputRouter (autoload Node, not Control). |
| ADR-0005 ↔ ADR-0004 Map/Grid `tile_grid_runtime_state` | ✅ no conflict | InputRouter reads `MapGrid.get_tile(coord)` for tap-to-select via Camera `screen_to_grid` indirection (provisional Camera contract per §9). Direct-call read pattern matches Map/Grid's query-first non-emitter discipline. |
| ADR-0005 ↔ ADR-0006 Balance/Data | ✅ no conflict | ADR-0005 reads `assets/data/input/default_bindings.json` directly via FileAccess — does NOT route through `BalanceConstants.get_const()`. Different config domain (input bindings vs tuning constants). 4-precedent JSON loading pattern (mirrors ADR-0006/0007/0008/0009) consistent. |
| ADR-0005 ↔ ADR-0007 Hero DB | ✅ no conflict | No direct dependency. |
| ADR-0005 ↔ ADR-0008 Terrain Effect | ✅ no conflict | No direct dependency. |
| ADR-0005 ↔ ADR-0009 Unit Role | ✅ no conflict | No direct dependency. |
| ADR-0005 ↔ ADR-0012 Damage Calc | ✅ no conflict | No direct dependency. (DamageCalc consumes events via cross-Feature Grid Battle bridge, not directly.) |

**ADR Dependency Ordering**: ADR-0005 depends on ADR-0001 + ADR-0002 + ADR-0004 (all Accepted). Soft / provisional downstream: Camera / Grid Battle / Battle HUD / Settings/Options / Tutorial (5 unwritten ADRs). No dependency cycles.

---

## Phase 5 — Engine Compatibility Audit

### Engine version + post-cutoff API consistency

| Check | Result |
|---|---|
| Engine version pin (4.6) | ✅ All 9 Accepted ADRs + ADR-0005 reference 4.6 |
| Post-cutoff API: 4.6 dual-focus | ✅ ADR-0005 §3 most-recent-event-class rule resolves; godot-specialist Item 1 PASS (event-class identity unchanged under dual-focus split) |
| Post-cutoff API: 4.5 SDL3 gamepad | ✅ ADR-0005 §6 KEYBOARD_MOUSE pass-through (no GAMEPAD MVP) |
| Post-cutoff API: 4.5 recursive Control disable | ✅ ADR-0005 §1 Node-based autoload (NOT Control) — recursive disable does not propagate to InputRouter; SceneManager uses set_process_input gate explicitly (godot-specialist Item 4 PASS) |
| Post-cutoff API: 4.5 Android edge-to-edge / safe-area | ⚠️ Implementation-time verification required (3-candidate API list per godot-specialist Item 5 — corrected this pass to add `DisplayServer.get_display_safe_area()` candidate); §Verification Required §5b mandatory before first story |
| Deprecated API references | ✅ Zero — no entries from `deprecated-apis.md` referenced in ADR-0005 |
| Engine Compatibility section present | ✅ Present + Knowledge Risk Note + 6 Verification Required items |

### godot-specialist Independent Validation (review-time, fresh-context)

**Verdict: APPROVED WITH SUGGESTIONS** — 8/8 items addressed; 7 PASS + 3 CONCERN→corrected + 2 advisories carried.

| Item | Topic | Verdict | Action |
|---|---|---|---|
| 1 | Module form Autoload Node justification | PASS | None |
| 2 | Dual-focus reconciliation + `_unhandled_input` ordering | PASS | None |
| 3 | InputMap.action_add_event matching by keycode/button (NOT pressed state); newly-constructed InputEvent default `pressed=false` | CONCERN→corrected | ADR-0005 §4 line 187 inline note added; verification §7 added |
| 4 | Inline match-dispatch state machine + re-entrancy hazard | PASS WITH ADVISORY | Implementation Notes Advisory D added |
| 5 | §7 Safe-area API name verification | CONCERN→corrected | ADR-0005 §7 candidate list expanded to 3 (added `get_display_safe_area`) |
| 6 | `DisplayServer.screen_get_size()` logical vs physical pixels | PASS | Existing §Verification §5a retained (no change) |
| 7 | DI test seam `before_test()` reset list — `_bindings.clear()` missing | CONCERN→corrected | ADR-0005 §8 line 225 + Validation §5 line 465 both updated |
| 8 | Cross-ADR consistency with ADR-0001 line 252 batching | PASS | None |
| 9 | ADR-0005 line 476 "MUST update to remove input-handling" — factually wrong | CONCERN→corrected | ADR-0005 line 476 + registry/architecture.yaml line 425 both rewritten |
| 10a | Signal type mismatch ADR-0001 vs ADR-0005 (String vs StringName) | CONCERN→advisory | Carried for next ADR-0001 amendment |
| 10b | `set_input_as_handled()` semantics in 4.x | PASS | None |
| 10c | `_pre_menu_state` inline comment imprecision | ADVISORY | Implementation Notes Advisory E added; line 126 inline comment refreshed |

**Pattern note**: 6 corrections this delta is above the 5-prior-delta average (~2 corrections / delta). Above-average count attributed to: (a) HIGH engine risk surface (3 post-cutoff areas), (b) longer ADR (484 lines pre-corrections), (c) first non-stateless-static module form ADR — pattern boundary needed wording precision.

---

## Phase 5b — GDD Revision Flags

**No revision flags.** All GDD assumptions consistent with verified engine behaviour. The GDD predates the dual-focus engine change (2026-04-16 vs 4.6 release Jan 2026) but the auto-detect "last-device-wins" rule (CR-2) was design-anticipated to be event-class-driven, not focus-channel-driven — this turns out to be coherent with 4.6 dual-focus per godot-specialist Item 1 PASS. No GDD prose needs amendment beyond the Status header refresh applied this pass.

---

## Phase 6 — Architecture Document Coverage

**No orphans.** ADR-0005 covers exactly the architecture-side surface described in `architecture.md` v0.4 lines 155 (Foundation Input Handling row), 193 (HIGH engine risk Foundation invariant), 285 (dual-focus × auto-detect cross-Foundation invariant), 318 (ADR-0001 line 372 prose drift advisory — separate item from delta #6 Item 9), 344 (Required ADR-0005 listing), 398 (closed Open Question on dual-focus × CR-2 coherence). Foundation layer transitions **4/5 → 5/5 Complete** upon this delta's Acceptance write.

---

## Phase 7 — Verdict

**PASS** with all 6 pre-Acceptance corrections applied same-patch; 2 advisories carried as Implementation Notes D + E; 1 post-Acceptance ADR-0001 amendment queued (line 168 String → StringName).

### Layer status (post-acceptance)

| Layer | Pre-Delta | Post-Delta |
|---|---|---|
| Platform | 3/3 Complete | 3/3 Complete |
| Foundation | 4/5 (ADR-0004/0006/0007/0009 Accepted; ADR-0005 Proposed) | **5/5 Complete** ✅ |
| Core | 1/2 | 1/2 (HP/Status ADR-0010 + Turn Order ADR-0011 still gap) |
| Feature | 1/3 | 1/3 (AI / Grid Battle / Destiny Branch still gap) |
| Presentation | 0/1 | 0/1 (Dual-focus UI pattern ADR still gap) |
| Polish | 0/1 | 0/1 (Accessibility AccessKit still gap) |

**Net-new ADRs required before Pre-Production → Production gate**: 3-7 (down from 4-8 pre-delta — ADR-0005 closes Foundation-layer gap entirely).

### Verdict for the Project

The architecture is **now coherent across the entire Foundation layer**. All 5 Foundation systems (Map/Grid + Hero DB + Balance/Data + Unit Role + Input Handling) have Accepted ADRs with TR-registered coverage. The next architectural critical path is the Core layer: ADR-0010 HP/Status (Damage Calc soft-dep `get_modified_stat`) + ADR-0011 Turn Order (Damage Calc soft-dep `get_acted_this_turn` + AI signal-inversion contract resolution).

---

## Phase 8 — Writes Performed (Same-Patch)

1. ✅ **`docs/architecture/ADR-0005-input-handling.md`** — Status Proposed → Accepted (header + Last Verified refresh) + 5 in-line corrections (line 126 `_pre_menu_state` comment refresh; line 187 `InputMap.action_add_event` matching note; line 214 §7 safe-area 3-candidate list; line 225 §8 DI seam `_bindings.clear()` + `_pre_menu_state` reset addition; line 332 Implementation Notes D + E added; line 465 Validation §5 reset list; line 476 §Related Decisions Item 9 factual correction).
2. ✅ **`docs/architecture/architecture-review-2026-04-30b.md`** — this report file (NEW).
3. ✅ **`docs/architecture/tr-registry.yaml`** v7 → v8 + 16 TR-input-handling-002..017 entries appended.
4. ✅ **`docs/architecture/architecture-traceability.md`** v0.6 → v0.7 — Foundation 4/5 → 5/5 Complete; Coverage Summary refresh; Registered TR-to-ADR map +16 rows; pending TR baseline (input-handling 11 candidates → REGISTERED 2026-04-30 collapse); Changelog row append.
5. ✅ **`docs/registry/architecture.yaml`** v2 → v3 — `input_action_dispatch` notes field rewritten (factual correction per delta #6 Item 9; new advisory Item 10a queued for ADR-0001 amendment).
6. ✅ **`design/gdd/input-handling.md`** — Status header refresh "Designed" → "Accepted via ADR-0005 (Proposed 2026-04-30 → Accepted 2026-04-30 via /architecture-review delta #6)" + Last Updated annotation.
7. ✅ **`production/session-state/active.md`** — silent session extract append (gitignored).

**No append to `docs/consistency-failures.md`** — no 🔴 CONFLICT entries (only GAP / precision findings, which the protocol excludes).

---

## Phase 9 — Handoff

### Immediate next-session candidates (priority order)

1. **`/architecture-decision hp-status` (ADR-0010)** — Core gap; soft-dep'd by ADR-0012 (`get_modified_stat` interface). Next critical-path Core ADR. DoT pipeline + DEFEND_STANCE / EXHAUSTED mutual-exclusion contract + morale system.
2. **`/architecture-decision turn-order` (ADR-0011)** — Core gap; resolves Blocker §1 (Turn Order → AI signal-inversion contract); soft-dep'd by ADR-0012 (`get_acted_this_turn` interface).
3. **`/sprint-plan` sprint-2** — formalize scope with 3 Foundation epics now eligible (Input Handling + Hero DB Battle Prep + completed unit-role bridge) + 2 Core epics (hp-status / turn-order; ADR-0010/0011 dependent).
4. **`/create-epics input-handling`** — eligible now that ADR-0005 is Accepted; brings Foundation layer to 5/5 epic-eligible.
5. **`/create-epics hero-database`** — eligible since ADR-0007 Accepted 2026-04-30; from prior next-session list still relevant.
6. **Next ADR-0001 amendment** — when next ADR-0001 substantive edit occurs (signal addition / payload ratification), batch the 2 carried advisories: line 168 `action: String` → `action: StringName` (delta #6 Item 10a) + line 372 prose drift `hero_database.get(unit_id)` → `HeroDatabase.get_hero(hero_id: StringName)` (ADR-0007 delta carried).

### Gate guidance

When the 4-8 net-new ADRs (now 3-7 post-delta) are written, run `/gate-check pre-production` to advance to Production phase. Foundation layer is no longer the bottleneck.

### Re-run trigger

Re-run `/architecture-review` after each new ADR is written to verify coverage improves. The delta-mode review pattern is now stable at 6 invocations (avg ~2 wording corrections per delta; this delta's 6 corrections were anomalously high due to HIGH-risk first-of-its-kind ADR surface).
