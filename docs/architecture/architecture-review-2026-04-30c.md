# Architecture Review — Delta #7 (ADR-0010 HP/Status)

> Generated by `/architecture-review ADR-0010` on 2026-04-30 (lean delta-mode review focused on Proposed → Accepted escalation of newly-authored ADR-0010). Disambiguation suffix `c` distinguishes from `architecture-review-2026-04-30.md` (ADR-0007 delta) and `architecture-review-2026-04-30b.md` (ADR-0005 delta) earlier same date.

| Field | Value |
|---|---|
| Mode | delta (single-ADR Proposed → Accepted escalation) |
| Date | 2026-04-30 |
| Engine | Godot 4.6 (pinned 2026-04-16; reference docs verified 2026-02-12) |
| ADR under review | ADR-0010 HP/Status — HPStatusController Battle-Scoped Node + Per-Unit RefCounted State (MVP scope) |
| GDDs Reviewed | design/gdd/hp-status.md (Designed 2026-04-16; Status header refreshed this pass; CR-5b apply_status 4-arg signature already synced 2026-04-30 to ADR-0010 §5 prior to this review) |
| ADRs Reviewed | 10 Accepted (ADR-0001..0009 + 0012) for cross-conflict scan + ADR-0010 (Proposed → Accepted this pass) |
| Verdict | **PASS** (with 3 pre-Acceptance precision-gap corrections applied same-patch; 2 advisories carried as Implementation Notes; 1 cross-doc unit_id type advisory queued for next ADR-0012 amendment) |
| Pattern invocation | 7th /architecture-review delta in pattern-stable invocation track (avg ~2.7 corrections / delta — this delta = 3 corrections, slightly above average for first Core-layer stateful battle-scoped Node form) |

---

## Phase 1 — Loaded Inputs

**Documents read in full**:
- `docs/architecture/ADR-0010-hp-status.md` (774 lines pre-corrections)
- `design/gdd/hp-status.md` (787 lines)
- `docs/engine-reference/godot/{VERSION,breaking-changes,deprecated-apis,current-best-practices}.md` (godot-specialist Phase 5 read)
- `docs/architecture/{ADR-0001-gamebus-autoload, ADR-0004-map-grid-data-model, ADR-0006-balance-data, ADR-0007-hero-database, ADR-0009-unit-role, ADR-0012-damage-calc}.md` (specifically the cross-ref regions: ADR-0001 lines 155 + 159 + 252 + 303-307 + 365-376; ADR-0012 lines 89-93 + 260 + 297-299 + 340-352; ADR-0009 line 166 + 328; ADR-0006 §6 G-15 obligation)
- `docs/architecture/tr-registry.yaml` (v8 → v9 this pass)
- `docs/architecture/architecture-traceability.md` (v0.7 → v0.8 this pass)
- `docs/architecture/architecture.md` (v0.4 → v0.5 partial-update this pass)
- `docs/registry/architecture.yaml` (already v3 → v4 from ADR-0010 Proposed-time commit `78efdb4`; no further bump this pass)
- `docs/architecture/control-manifest.md` (no change this pass — first story implementation will add HP/Status control rules)
- `.claude/docs/technical-preferences.md`

---

## Phase 2 — Technical Requirements Extracted from GDD

`design/gdd/hp-status.md` authored 2026-04-16 with 9 Core Rules CR-1..CR-9 (CR-1 HP lifecycle non-persistence + CR-1a/b/c sub-rules; CR-2 HP range invariant external-write ban; CR-3 4-step damage intake pipeline; CR-4 4-step healing pipeline + CR-4a/b/c; CR-5 6-field StatusEffect schema + CR-5a..f sub-rules; CR-6 5 MVP status effects SE-1..SE-5 with explicit duration types + tick effects + apply/release conditions; CR-7 DEFEND_STANCE+EXHAUSTED mutex; CR-8 8a..e Death + DEMORALIZED propagation; CR-9 9a..c MIN_DAMAGE floor + overkill discard); 4 formulas F-1..F-4; 17 Edge Cases EC-01..EC-17 grouped by 6 categories; 27 Tuning Knobs; 20 Acceptance Criteria AC-01..AC-20; 6 Open Questions OQ-1..OQ-6 (4 partially resolved this ADR — OQ-1 resurrection / OQ-2 HP carry-over / OQ-3 additional effects / OQ-6 DoT type extension; OQ-4 is_morale_anchor + OQ-5 nested propagation deferred post-MVP).

The traceability index pre-state listed 11 candidate TRs under hp-status.md baseline. This pass collapses the candidate list and registers **17 net-new TR-hp-status-002..018** entries (the 11 candidates + 6 architecture-emergent TRs covering R-1..R-7 risks + cross-ADR consumer contracts + cross-doc unit_id type advisory).

---

## Phase 3 — Traceability Matrix

### Pre-state (registry v8 baseline)

| TR-ID | System | ADR | Status | Coverage |
|---|---|---|---|---|
| TR-hp-status-001 | hp-status | ADR-0001 | Accepted | ✅ Covered (signal exposure via GameBus, registered 2026-04-18 with ADR-0001 acceptance) |

### Post-state (registry v9 — 17 net-new TR-hp-status-002..018 registered this pass)

| TR-ID | ADR-0010 §Section | Coverage | Summary |
|---|---|---|---|
| TR-hp-status-002 | §1 Module form | ✅ Covered | Battle-scoped Node child of BattleScene (lifecycle alignment with ADR-0004); 5-precedent stateless-static rejected as Alternative 3 (engine-incompatible — stateful + signal-listening); Autoload rejected as Alternative 1 (CR-1b non-persistence); per-unit Component rejected as Alternative 2 (cross-unit query overhead) |
| TR-hp-status-003 | §3 UnitHPState schema | ✅ Covered | RefCounted (NOT Resource) — 6 fields: unit_id int / max_hp int / current_hp int / status_effects Array[StatusEffect] / hero HeroData ref / unit_class int; matches ADR-0012 4-RefCounted-wrapper precedent (per-call non-persistent) |
| TR-hp-status-004 | §4 StatusEffect + TickEffect | ✅ Covered | Typed Resource StatusEffect with 7 @export fields + separate TickEffect Resource for DoT formula reuse; 5 .tres templates in assets/data/status_effects/; shallow `.duplicate()` intentional per delta-#7 godot-specialist Item 2 (read-only sub-Resource pattern, NOT subject to deprecated-apis.md duplicate_deep mandate) |
| TR-hp-status-005 | §5 Public API + signals | ✅ Covered | 8 public methods (initialize_unit / apply_damage / apply_heal / apply_status / get_current_hp / get_max_hp / is_alive / get_modified_stat / get_status_effects); 1 emitted signal `unit_died(unit_id: int)` per ADR-0001 line 155 ratification; 1 consumed signal `unit_turn_started(unit_id: int)` per ADR-0011 provisional |
| TR-hp-status-006 | §6 + F-1 + EC-03 | ✅ Covered | Damage intake 4-step pipeline: Step 1 passive flat (SHIELD_WALL_FLAT for PHYSICAL+passive_shield_wall) → Step 2 status modifier (DEFEND_STANCE first per EC-03 bind-order, then VULNERABLE post-MVP) → Step 3 MIN_DAMAGE floor (dual-enforcement with Damage Calc per ADR-0012 line 92) → Step 4 HP reduction + unit_died emit AFTER mutation |
| TR-hp-status-007 | §7 + F-2 + CR-4a/b | ✅ Covered | Healing 4-step pipeline: raw_heal → EXHAUSTED multiplier max(1, floor) → overheal prevention `min(raw_heal, max_hp - current_hp)` → HP increase; CR-4a no-overheal + CR-4b dead-units-cannot-be-healed; returns actual heal_amount for UI feedback (skip "healed for 0" per EC-09) |
| TR-hp-status-008 | §8 + CR-5c/d/e + F-3 | ✅ Covered | Status effect lifecycle: CR-5c same-effect refresh (no stack); CR-5d different-effect coexist; CR-5e MAX_STATUS_EFFECTS_PER_UNIT=3 with `pop_front()` insertion-order eviction; DoT-then-decrement order per GDD §States and Transitions line 243-245; reverse-index `while i >= 0` removal pattern for TURN_BASED expiry per delta-#7 godot-specialist Item 7 PASS |
| TR-hp-status-009 | §9 + F-4 + EXHAUSTED special-case | ✅ Covered | F-4 modifier application: sum modifier_targets[stat_name] across active effects → clamp [MODIFIER_FLOOR, MODIFIER_CEILING] → `max(1, int(floor(base × (1 + total_modifier / 100.0))))` (Item 9 explicit int cast applied this pass); EXHAUSTED move-range special-case branch (flat -1, NOT percent — schema asymmetry documented); DEFEND_STANCE_ATK_PENALTY pre-folded via modifier_targets per ADR-0012 line 89-93 contract |
| TR-hp-status-010 | §10 + CR-7 | ✅ Covered | DEFEND_STANCE + EXHAUSTED mutex enforcement: EXHAUSTED → DEFEND_STANCE attempt returns false ("피로로 태세 유지 불가" UI feedback); DEFEND_STANCE → EXHAUSTED apply force-removes DEFEND_STANCE BEFORE appending EXHAUSTED |
| TR-hp-status-011 | §11 + CR-8 + R-6 | ✅ Covered | Death + DEMORALIZED propagation: unit_died emit AFTER current_hp=0 mutation (Verification §5); Commander class auto-trigger radius scan via `_propagate_demoralized_radius`; is_morale_anchor branch DEFERRED post-MVP per OQ-2 (HeroData schema gap); MapGrid DI for radius scan via explicit `_map_grid: MapGrid` field (NOT autoload lookup); R-6 mitigation: `_propagate_demoralized_radius` invoked from BOTH `apply_damage` Step 4 AND `_apply_turn_start_tick` DoT-kill branch when unit_class == COMMANDER |
| TR-hp-status-012 | §12 + 27 BalanceConstants | ✅ Covered | All 27 tuning knobs routed through `BalanceConstants.get_const(key)` per ADR-0006 5-precedent JSON pattern; `assets/data/balance/balance_entities.json` 27-key append is story-level same-patch obligation (mirrors ADR-0007 §Migration §4 + ADR-0009 §Migration §From provisional precedents); `tools/ci/lint_balance_entities_hp_status.sh` validates presence + safe-range; DEFEND_STANCE_ATK_PENALTY=-40 documented as provisional INERT per grid-battle v5.0 CR-13 rule 4 |
| TR-hp-status-013 | §13 + Validation §5 | ✅ Covered | DI test seam `_apply_turn_start_tick(unit_id)` direct call bypasses GameBus signal subscription; `_map_grid` constructor-injected via Battle Preparation; `before_test()` G-15 reset: `BalanceConstants._cache_loaded = false` + fresh `HPStatusController.new()` + MapGridStub injection; mirrors ADR-0005 `_handle_event` + ADR-0012 ResolveModifiers.rng 4-precedent DI seam pattern |
| TR-hp-status-014 | §1 + R-1 | ✅ Covered | Re-entrant `unit_died` emission during DEMORALIZED propagation: ADR-0001 §5 mandate `CONNECT_DEFERRED` for cross-scene subscribers eliminates synchronous re-entrancy; `_state_by_unit.keys()` snapshot semantics safe per delta-#7 godot-specialist Item 6 PASS (Array snapshot, not live view); same-scene subscriber forbidden_pattern `hp_status_re_entrant_emit_without_deferred`; unit test fixture asserts re-entrancy safe-or-explicitly-forbidden |
| TR-hp-status-015 | R-5 + Validation §12 | ✅ Covered | Consumer mutation forbidden_pattern `hp_status_consumer_mutation` (mirrors ADR-0007 hero_data_consumer_mutation precedent); `get_status_effects` returns shallow Array copy but contained StatusEffect Resources are SHARED references; documented test asserts mutation IS visible cross-call (proving convention is sole defense) — fail-state regression test, NOT passing protective test |
| TR-hp-status-016 | §Performance + Validation §8 | ✅ Covered | apply_damage <0.05ms / get_modified_stat <0.05ms / apply_status <0.10ms / `_apply_turn_start_tick` <0.20ms (max 3 status_effects × DoT/decrement); heap footprint <10KB << 512MB ceiling (24-unit max battle × 120-byte UnitHPState + 80-byte StatusEffect × 3 max + 5×500-byte template = ~12KB); on-device measurement deferred per damage-calc story-010 Polish-deferral pattern (stable at 6+ invocations) |
| TR-hp-status-017 | §Verification §1 + §9 | ✅ Covered | Cross-platform determinism: same `apply_damage / apply_heal / apply_status / _apply_turn_start_tick` sequence produces identical state on macOS Metal + Linux Vulkan + Windows D3D12; integer arithmetic + deterministic `floor()` semantics (no float-point math except `ceil(max_hp × HEAL_HP_RATIO)` and `floor(max_hp × DOT_HP_RATIO)` — both float-input → integer-output); headless CI deterministic-fixture test mandatory before Polish |
| TR-hp-status-018 | §ADR Dependencies cross-doc + §Negative + §Related | ✅ Covered | unit_id type lock at `int` to match ADR-0001 line 155 `unit_died(unit_id: int)` signal-contract source-of-truth; ADR-0012 lines 89/260 use `unit_id: StringName` — precision-gap cross-doc advisory queued for next ADR-0012 amendment alongside the 2 prior carried advisories (ADR-0001 line 168 String → StringName + line 372 prose drift `hero_database.get(unit_id)` → `HeroDatabase.get_hero(hero_id: StringName)`); migration parameter-stable (int and StringName not auto-coercible); call sites in ADR-0012 prose pseudocode line 352-353 need explicit `int` typing in actual implementation |

**Coverage**: 18/18 hp-status TRs covered (1 prior + 17 new). 0 gaps.

---

## Phase 4 — Cross-ADR Conflict Scan

**Result: 0 BLOCKING conflicts. 1 cross-doc precision-gap advisory carried (queued for next ADR-0012 amendment).**

Pairwise scan against the 10 Accepted ADRs:

| Pair | Conflict found? | Notes |
|---|---|---|
| ADR-0010 ↔ ADR-0001 GameBus | ✅ no conflict | `unit_died(unit_id: int)` emitter contract ratified verbatim (ADR-0001 line 155); HP/Status Domain emitter declaration line 303-307 ratified by ADR-0010 §1 module form choice; non-emitter for all OTHER 21 GameBus signals confirmed by Validation §4 grep gate |
| ADR-0010 ↔ ADR-0001 batching obligation (line 252) | ✅ no conflict | HP/Status signals are bursty (per-attack, per-DoT-tick), NOT high-frequency (NOT per-frame); ADR-0001 batching rule applies only to InputRouter-style frame-coupled events |
| ADR-0010 ↔ ADR-0001 non-emitter list | ✅ no conflict | HP/Status is on the emitter side for `unit_died` only (ADR-0001 line 303-307); non-emitter by behavior for other 21 signals across 8 domains; consistent with ADR-0001 line 365-376 non-emitter list (HP/Status NOT listed because it IS an emitter for unit_died) |
| ADR-0010 ↔ ADR-0002 SceneManager | ✅ no conflict | HPStatusController is battle-scoped Node child of BattleScene; freed automatically with BattleScene per `_free_battle_scene()` automatic teardown — matches ADR-0002 SceneManager lifecycle pattern; no cross-battle state survival required (CR-1b) |
| ADR-0010 ↔ ADR-0004 Map/Grid | ✅ no conflict | HPStatusController calls `MapGrid.get_tile(coord: Vector2i) -> TileData.occupant_id` for DEMORALIZED radius scan (CR-6 SE-2 condition (a)); registry line 293 already documents this consumer pattern; battle-scoped lifecycle alignment (both Node, both freed with BattleScene) |
| ADR-0010 ↔ ADR-0006 Balance/Data | ✅ no conflict | All 27 tuning knobs read via `BalanceConstants.get_const(key) -> Variant` per ADR-0006 5-precedent pattern; G-15 `_cache_loaded = false` reset obligation in every HPStatusController test suite; story-level same-patch obligation for `balance_entities.json` 27-key append mirrors ADR-0007 §Migration §4 precedent |
| ADR-0010 ↔ ADR-0007 Hero DB | ✅ no conflict (1 deferred soft-dep) | `HeroDatabase.get_hero(hero_id: StringName) -> HeroData` provides `base_hp_seed` (transitively consumed via UnitRole.get_max_hp); `is_morale_anchor: bool` field NOT in ratified 26-field schema (verified via grep — zero matches); MVP DEMORALIZED triggers ONLY via condition (a) Commander class + (c) direct skill apply; condition (b) is_morale_anchor heroes deferred post-MVP per OQ-2 (next ADR-0007 amendment OR ADR-0014 Scenario Progression) |
| ADR-0010 ↔ ADR-0008 Terrain Effect | ✅ no conflict | No direct dependency. Terrain modifiers are consumed by Damage Calc via `get_combat_modifiers`; HP/Status only consumes the resolved damage value via `apply_damage(unit_id, resolved_damage, attack_type, source_flags)`. |
| ADR-0010 ↔ ADR-0009 Unit Role | ✅ no conflict | `UnitRole.get_max_hp(hero, unit_class) -> int` cached at battle-init for each unit's `UnitHPState.max_hp` field (one call per unit per battle per ADR-0009 line 328 cadence); `UnitRole.PASSIVE_TAG_BY_CLASS[unit_class]` for `passive_shield_wall` lookup in F-1 Step 1; `UnitRole.get_atk / get_phys_def / get_mag_def / get_initiative / get_effective_move_range` for F-4 base_stat dispatch in `get_modified_stat` |
| ADR-0010 ↔ ADR-0012 Damage Calc | ⚠️ 1 type-precision advisory (NON-blocking) | ADR-0012 lines 89/260 use `unit_id: StringName`; ADR-0010 locks `unit_id: int` to match ADR-0001 signal-contract source-of-truth. Auto-coercion does NOT occur in GDScript 4.x (StringName and int are not coercible). Carried advisory: ADR-0012 next amendment to align line 89/260 + line 352-353 pseudocode call sites to `unit_id: int`. Queues with the 2 prior carried advisories (ADR-0001 line 168 String → StringName + line 372 prose drift). All other ADR-0012 contracts ratified verbatim: `apply_damage` sole-Grid-Battle-caller (line 260); `get_modified_stat` interface (lines 89-93, 340-352); MIN_DAMAGE / ATK_CAP / DEF_CAP / DEFEND_STANCE_ATK_PENALTY ownership transferred to ADR-0010 §12; closes ADR-0012's only outstanding upstream Core-layer soft-dep among ADR-0010/0011 (ADR-0011 still provisional). |

**ADR Dependency Ordering**: ADR-0010 hard-depends on ADR-0001 + ADR-0004 + ADR-0006 + ADR-0007 + ADR-0009 + ADR-0012 (all Accepted). Soft / provisional: ADR-0011 Turn Order (NOT YET WRITTEN — `unit_turn_started` consumer contract; mirrors 5-precedent provisional-dep pattern); future ADR-0007 amendment OR ADR-0014 Scenario Progression for `is_morale_anchor` field; future Battle Preparation ADR for `initialize_unit` lifecycle hook. **No dependency cycles.**

---

## Phase 5 — Engine Compatibility Audit

### Engine version + post-cutoff API consistency

| Check | Result |
|---|---|
| Engine version pin (4.6) | ✅ All 10 Accepted ADRs + ADR-0010 reference 4.6 |
| Post-cutoff API: 4.4 typed `Dictionary[K, V]` | ✅ Production-stable in 4.6 (godot-specialist delta-#7 PASS Item 1 with same-patch correction — declare typed form directly, do NOT defer to "first story") |
| Post-cutoff API: 4.5 `@abstract` decorator | ✅ NOT used (HPStatusController is concrete Node, not abstract base) |
| Post-cutoff API: 4.5 `duplicate_deep()` | ✅ NOT used (intentional shallow `.duplicate()` for read-only sub-Resource pattern; godot-specialist delta-#7 Item 2 same-patch correction — clarified scope of `deprecated-apis.md` entry + added hot-reload behavior note) |
| Post-cutoff API: StringName literals `&"effect_id"` | ✅ Stable since 4.0; matches ADR-0007/0009/0012 4-precedent StringName convention |
| Deprecated API references | ✅ Zero — no entries from `deprecated-apis.md` referenced in ADR-0010 (delta-#7 Item 5 ADVISORY: string-form `emit_signal()` produces console deprecation warning — Validation §3 lint enforces typed `.emit(` form correctly) |
| Engine Compatibility section present | ✅ Present + Knowledge Risk Note + 5 Verification Required items |

### godot-specialist Independent Validation (review-time, fresh-context)

**Verdict: APPROVED WITH SUGGESTIONS** — 10/10 items addressed; 5 PASS + 3 CONCERN→corrected + 2 advisories carried.

| Item | Topic | Verdict | Action |
|---|---|---|---|
| 1 | `Dictionary[int, UnitHPState]` typed declaration consistency | CONCERN→corrected | ADR-0010 §1 line 119 declares typed form directly (drop "first story declares" deferral); diagram already used typed form so consistency restored |
| 2 | `duplicate()` shallow vs `duplicate_deep()` rationale + hot-reload behavior | CONCERN→corrected | ADR-0010 §4 footnote extended: clarified `deprecated-apis.md` entry refers to GENERAL pattern not read-only sub-Resource case; added editor-mode hot-reload behavior note (intentional for designer iteration) |
| 3 | `load(...) as StatusEffect` null-safety | PASS | None |
| 4 | BattleScene teardown / GC cycle hazard | PASS | None |
| 5 | `emit_signal()` deprecation form runtime behavior | ADVISORY | Note carried: string-form produces console deprecation warning (NOT runtime error) — explains why Validation §3 lint enforces `.emit(` suffix |
| 6 | `_state_by_unit.keys()` snapshot during DEMORALIZED iteration | PASS | None — `.keys()` returns Array snapshot, safe |
| 7 | Reverse-index `while i >= 0` removal pattern | PASS | None — idiomatic Godot 4.x |
| 8 | Lazy `load()` vs battle-init pre-warm for .tres templates | ADVISORY | Acceptable for MVP turn-based cadence (~5ms first-attack-of-each-effect-type stutter); revisit only if Polish profiling flags |
| 9 | `floor()` returns float; `get_modified_stat -> int` implicit coercion | CONCERN→corrected | ADR-0010 §6 line 250 (F-1 Step 2 `post_passive`) + §9 line 397 (F-4 final return) wrapped in explicit `int(floor(...))` cast; eliminates editor SAFE-mode implicit coercion warning + documents return-type honesty |
| 10 | `_map_grid` DI lifecycle ordering hazard | PASS | None — R-3 assert is sufficient defense-in-depth; no path where `_propagate_demoralized_radius` fires before Battle Preparation completes |

**Pattern note**: 3 corrections this delta is between the 5-prior-delta avg (~2) and ADR-0005's anomalously-high 6. Above-average attributable to: (a) Core-layer first-of-its-kind stateful battle-scoped Node form (vs. 5 prior stateless-static + 1 stateful Autoload); (b) numeric coercion precision (`floor()` returns float in 4.x); (c) typed Dictionary declaration consistency between code block and architecture diagram. NOT engine risk — wording precision.

---

## Phase 5b — GDD Revision Flags

**No revision flags.** All GDD assumptions consistent with verified engine behaviour. The GDD was already synced 2026-04-30 to ADR-0010 §5 4-arg `apply_status(unit_id, effect_template_id, duration_override, source_unit_id)` signature (CR-5b explicit "Synced 2026-04-30" annotation line 137-138). Status header refresh in this pass: `Designed` → `Accepted via ADR-0010 (Proposed 2026-04-30 → Accepted 2026-04-30 via /architecture-review delta #7)`.

---

## Phase 6 — Architecture Document Coverage

**No orphans.** ADR-0010 covers exactly the architecture-side surface described in `architecture.md` v0.4 line 158 (Core HP/Status System #12 row), 209 (Required ADRs deferred list — ADR-0010 to be removed post-Acceptance), 257 (Phase 2 Module Ownership Core layer HP/Status row — status flip from "⏳ ADR needed for status-effect stacking contract" to "✅ ADR-0010 Accepted"), 318 (Phase 5 ADR Audit table — ADR-0010 row append), 340 (Layer status line refresh), 345 (gap call-out — to be removed), 368 (mandatory list pruned 3 → 2). **Layer status post-acceptance: Foundation 5/5 + Core 1/2 (HP/Status ✅; Turn Order ADR-0011 still gap; Note: ADR-0010 §Ordering Note reclassifies ADR-0008 Terrain Effect as Foundation calculator pattern — kept consistent with prior wording in this pass; deeper architecture.md re-classification deferred to ADR-0011 next pass).**

---

## Phase 7 — Verdict

**PASS** with all 3 pre-Acceptance corrections applied same-patch; 2 advisories carried as Implementation Notes; 1 cross-doc unit_id type advisory queued for next ADR-0012 amendment.

### Layer status (post-acceptance)

| Layer | Pre-Delta | Post-Delta |
|---|---|---|
| Platform | 3/3 Complete | 3/3 Complete |
| Foundation | 5/5 Complete | 5/5 Complete |
| Core | 1/2 (ADR-0008 Accepted; ADR-0010 Proposed) | **2/2** with HP/Status ✅; ADR-0011 Turn Order still gap |
| Feature | 1/3 | 1/3 (AI / Grid Battle / Destiny Branch still gap) |
| Presentation | 0/1 | 0/1 (Dual-focus UI pattern ADR still gap) |
| Polish | 0/1 | 0/1 (Accessibility AccessKit still gap) |

**Net-new ADRs required before Pre-Production → Production gate**: 2-6 (down from 3-7 pre-delta — ADR-0010 closes the highest-leverage Core-layer gap). Next critical-path ADR is ADR-0011 Turn Order (Damage Calc soft-dep `get_acted_this_turn` + AI signal-inversion contract resolution).

### Verdict for the Project

The architecture is **now coherent across Foundation + first-half of Core**. ADR-0010 closes ADR-0012's only outstanding upstream Core-layer soft-dep (`get_modified_stat` + `apply_damage` interfaces ratified verbatim from ADR-0012 line 89-93/260). The next architectural critical path is ADR-0011 Turn Order, which closes ADR-0012's remaining soft-dep (`get_acted_this_turn`) AND resolves the Turn Order → AI signal-inversion contract referenced in `architecture.md` blocker §1.

---

## Phase 8 — Writes Performed (Same-Patch)

1. ✅ **`docs/architecture/ADR-0010-hp-status.md`** — Status `Proposed` → `Accepted` (header + Last Verified refresh) + 3 in-line corrections (Item 1 §1 line 119 typed Dictionary declaration; Item 2 §4 footnote extension with hot-reload note + deprecated-apis.md scope clarification; Item 9 §6 line 250 + §9 line 397 explicit `int(floor(...))` casts).
2. ✅ **`docs/architecture/architecture-review-2026-04-30c.md`** — this report file (NEW).
3. ✅ **`docs/architecture/tr-registry.yaml`** v8 → v9 + 17 TR-hp-status-002..018 entries appended.
4. ✅ **`docs/architecture/architecture-traceability.md`** v0.7 → v0.8 — Foundation 5/5 (unchanged) + Core 1/2 → 2/2 (ADR-0010 Accepted); Coverage Summary refresh; Registered TR-to-ADR map +17 rows; pending TR baseline (hp-status 11 candidates → REGISTERED 2026-04-30 collapse); Changelog row append.
5. ✅ **`docs/architecture/architecture.md`** v0.4 → v0.5 partial-update — Document Status (ADRs Referenced 9 → 10); Phase 2 HP/Status Module Ownership row status flip (✅ ADR-0010 Accepted); Phase 5 ADR Audit ADR-0010 row append; Phase 6 mandatory list pruned 3 → 2 (ADR-0010 removed); Layer status line refresh; changelog row append.
6. ✅ **`design/gdd/hp-status.md`** — Status header refresh `Designed` → `Accepted via ADR-0010 (Proposed 2026-04-30 → Accepted 2026-04-30 via /architecture-review delta #7)` + Last Updated annotation.
7. ✅ **`production/session-state/active.md`** — silent session extract append (gitignored).

**No append to `docs/consistency-failures.md`** — no 🔴 CONFLICT entries (only GAP / precision findings + 1 cross-doc precision-gap advisory, all of which the protocol excludes from the reflexion log).

**No update to `docs/registry/architecture.yaml`** — already bumped v3 → v4 in ADR-0010 Proposed-time commit `78efdb4` 2026-04-30 with 5 hp-status entries (state + interfaces + api_decisions + forbidden_patterns).

**No update to `docs/architecture/control-manifest.md`** — first hp-status implementation story will append HP/Status control rules; manifest version date refresh deferred to that story.

---

## Phase 9 — Handoff

### Immediate next-session candidates (priority order)

1. **`/architecture-decision turn-order` (ADR-0011)** — last Core-layer gap; closes ADR-0012's `get_acted_this_turn` soft-dep + resolves Turn Order → AI signal-inversion contract per `architecture.md` blocker §1. Brings Core layer 2/2 → 3/3.
2. **`/create-epics hp-status`** — eligible now that ADR-0010 is Accepted; first Core-layer epic. ADR-0010 §Migration Plan §1-§7 + §Validation Criteria §1-§13 form the runbook; estimated 8-12 stories (5 status effect tests + F-1/F-2/F-3/F-4 formula tests + DEMORALIZED radius integration + DI seam test + 27 BalanceConstants append + cross-platform determinism + re-entrancy regression).
3. **`/create-epics input-handling`** — eligible since ADR-0005 Accepted 2026-04-30 (delta #6); from prior next-session list still relevant.
4. **`/create-epics hero-database`** — eligible since ADR-0007 Accepted 2026-04-30 (Foundation delta); from prior next-session list still relevant.
5. **`/sprint-plan sprint-2`** — formalize scope with 4 epic-eligible candidates (hp-status / input-handling / hero-database / scene-manager remainder) + 1 ADR-pending (turn-order) + Foundation 5/5 + Core 1/2 critical-path tracking.
6. **Next ADR-0012 amendment** — when next ADR-0012 substantive edit occurs, batch the **3 carried advisories**: (a) line 89/260/352-353 `unit_id: StringName` → `unit_id: int` (delta #7 Phase 4 Pair ADR-0010↔ADR-0012); (b) line 168 `action: String` → `action: StringName` (delta #6 Item 10a — re-confirmed cross-doc carry); (c) line 372 prose drift `hero_database.get(unit_id)` → `HeroDatabase.get_hero(hero_id: StringName)` (delta #5 ADR-0007 carry).

### Gate guidance

When the 2-6 net-new ADRs (now 2-6 post-delta — Core layer 1 gap remaining; Feature 2; Presentation 1; Polish 1) are written, run `/gate-check pre-production` to advance to Production phase. Foundation layer is no longer the bottleneck. Core layer at 2/2 closes the highest-leverage gameplay-loop ADR coverage; remaining gaps are Feature/Presentation/Polish.

### Re-run trigger

Re-run `/architecture-review` after each new ADR is written to verify coverage improves. The delta-mode review pattern is now stable at 7 invocations (avg ~2.7 wording corrections per delta; this delta's 3 corrections fall within the established range — slightly above mean, attributable to first stateful battle-scoped Node form needing wording precision around typed Dictionary + numeric coercion).
