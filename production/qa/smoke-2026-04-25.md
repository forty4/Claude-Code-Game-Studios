# Smoke Check Report

**Date**: 2026-04-25
**Sprint / Epic**: map-grid (8/8 stories Complete)
**Engine**: Godot 4.6
**QA Plan**: not found for map-grid (only `qa-plan-save-manager-2026-04-24.md` exists; map-grid epic relied on per-story QA test cases authored inline in lean mode)
**Argument**: sprint
**Branch**: `chore/map-grid-td-032-adr-errata` (post-errata, includes all stories 005-008 + ADR/spec errata pass)

---

## Automated Tests

**Status**: ✅ **PASS** — 231 tests, 231 passing, 0 errors / 0 failures / 0 flaky / 0 skipped / 0 orphans, exit 0.

Test growth across the epic: **196 (epic baseline) → 231 (epic close)**, +35 new tests across stories 005-008.

### Suite breakdown

| Suite | Tests | Notes |
|-------|------:|-------|
| `tests/unit/core/map_resource_test.gd` (story-001 baseline) | (baseline) | Resource schema, typed-array constraints |
| `tests/unit/core/map_grid_test.gd` (story-002 baseline) | (baseline) | MapGrid skeleton + 6 packed caches + trivial queries |
| `tests/unit/core/map_grid_validation_test.gd` (story-003 baseline) | (baseline) | Validator + error collection |
| `tests/integration/core/map_grid_mutation_test.gd` (story-004 baseline) | (baseline) | Mutation API + cache write-through + `tile_destroyed` signal |
| `tests/unit/core/map_grid_pathfinding_test.gd` (story-005 NEW) | 16 | Dijkstra + V-4 50-query reference equivalence |
| `tests/unit/core/map_grid_queries_test.gd` (story-006 NEW) | 13 | LoS Bresenham + V-3 20-case matrix + 4 attack/adjacency queries |
| `tests/integration/core/map_grid_perf_test.gd` (story-007 NEW) | 3 | V-1 desktop perf baseline; AC-DESKTOP + AC-WARMUP + AC-FRAME-TIME |
| `tests/integration/core/map_grid_inspector_fixtures_test.gd` (story-008 NEW) | 2 | Inspector fixture validator pass smoke |
| Cross-system + non-map-grid (gamebus, scene-manager, save-manager, etc.) | (baseline) | Unchanged |

### Performance highlight

`get_movement_range(move_range=10)` on the 40×30 stress fixture (1200 tiles, 60% PLAINS / 15% HILLS / 10% FOREST / 10% ROAD / 5% MOUNTAIN, 5 enemy obstacles):

- p50 = 0.272 ms
- **p95 = 0.288 ms** (HARD threshold: 16 ms — desktop substitute for AC-PERF-2 mobile target)
- max = 0.376 ms
- mean = 0.270 ms
- warmup ratio = 1.08× (advisory threshold 3×)
- 0/100 frame-time over-budget; 0 2-consecutive runs; 0 3-consecutive runs

**~17× under ADR-0004's <5 ms desktop expectation; ~55× under AC-PERF-2 16 ms mobile target.** ADR-0004 R-1 fallbacks (map cap 32×24, flow-field precompute, GDExtension C++) NOT triggered; mobile remains plausible without invoking them.

---

## Test Coverage

| Story | Type | Test Evidence | Coverage Status |
|-------|------|---------------|----------------|
| 001 MapResource + MapTileData | Logic | `tests/unit/core/map_resource_test.gd` (baseline) | COVERED |
| 002 MapGrid skeleton + 6 caches | Logic | `tests/unit/core/map_grid_test.gd` (baseline) | COVERED |
| 003 Loading validation + errors | Logic | `tests/unit/core/map_grid_validation_test.gd` (baseline) | COVERED |
| 004 Mutation API + signal | Integration | `tests/integration/core/map_grid_mutation_test.gd` (baseline) | COVERED |
| 005 Custom Dijkstra | Logic | `tests/unit/core/map_grid_pathfinding_test.gd` (16 tests + V-4 50-query equivalence) | COVERED |
| 006 LoS + attack queries | Logic | `tests/unit/core/map_grid_queries_test.gd` (13 tests + V-3 20-case matrix) | COVERED |
| 007 Perf baseline | Integration | `tests/integration/core/map_grid_perf_test.gd` (3 tests, V-1 desktop) | COVERED |
| 008 Inspector authoring | UI | `tests/integration/core/map_grid_inspector_fixtures_test.gd` (programmatic 4 ACs) + `production/qa/evidence/map-grid-inspector-v7.md` (manual 3 ACs PENDING USER SIGN-OFF) | MANUAL |

**Summary**: 7 COVERED, 1 MANUAL (story-008 pending user sign-off in evidence doc), 0 MISSING, 0 EXPECTED.

---

## Manual Smoke Checks

**Status: N/A — Foundation-only epic, no playable surface area**

The map-grid epic delivers a **backend Foundation system**: data model (`MapResource` / `MapTileData`), validator, mutation API, pathfinding (Dijkstra), LoS (Bresenham), 9 query methods. None of these are user-visible — they are **consumed by future Core/Feature epics** (Grid Battle, AI, Formation Bonus, Damage Calc) that haven't been started yet.

The standard smoke batches (game launches → main menu → start new game → save/load → frame-rate hitches) **cannot run** because:

- **Game does not launch** to a main menu (no main scene wired up yet — that's a future Presentation/Polish epic concern).
- **No gameplay loop exists** to exercise the new queries.
- **Save system is independent** (save-manager epic; not consumed by map-grid).
- **Performance is auto-checked** by `map_grid_perf_test.gd::test_map_grid_perf_frame_time_no_three_consecutive_above_16ms` (no 3-consecutive-sample >16.6 ms across 100 sequential queries on the 40×30 stress fixture; 0/100 over-budget on dev machine).

### Substitute checks (all PASS)

| Verification | Method | Result |
|--------------|--------|--------|
| AC-PERF-2 (`get_movement_range` p95 <16ms on 40×30 m=10) | `map_grid_perf_test.gd::test_..._p95_under_desktop_budget` | ✅ p95=0.288ms (~55× under target) |
| V-3 LoS 20-case matrix | `map_grid_queries_test.gd::test_..._v3_matrix_20_cases` | ✅ 20/20 |
| V-4 Dijkstra 50-query reference equivalence | `map_grid_pathfinding_test.gd::test_..._reference_equivalence_50_queries` | ✅ 50/50 |
| V-5 cache-sync per mutation | `map_grid_mutation_test.gd` | ✅ |
| V-6 signal emission (`tile_destroyed`) | `map_grid_mutation_test.gd` | ✅ |
| V-7 (40×30 inspector load without hang — programmatic) | `map_grid_inspector_fixtures_test.gd::test_..._stress_40x30_loads_and_validates` | ✅ |
| V-7 (40×30 inspector load without hang — **manual stopwatch**) | `production/qa/evidence/map-grid-inspector-v7.md` Manual AC-2 | 🟡 PENDING USER |
| V-1 mobile AC-TARGET on-device | DEFERRED to Polish phase per story-007 §7 | 🟡 DEFERRED |

---

## Missing Test Evidence

**None.** All Logic and Integration stories have automated test files; the UI story (008) has the evidence doc structure complete with manual portions explicitly tracked.

---

## Outstanding (post-smoke advisory)

### Story-008 manual sign-off

Open Godot 4.6 editor, perform 3 manual ACs in `production/qa/evidence/map-grid-inspector-v7.md`:

1. **AC-INSPECTOR-LOAD-TIME**: stopwatch the `data/maps/stress_40x30.tres` inspector load; target ≤30s. >30s triggers ADR-0004 R-1 fallback discussion.
2. **AC-EDIT-ROUND-TRIP**: edit `tiles[0].destruction_hp` to 777 in `sample_small.tres`, Ctrl+S, FileSystem-deselect/reselect to reload, verify persistence; revert; re-run integration smoke.
3. **AC-AUTHORING-DOC measurements + 2 screenshots**: fill placeholders in evidence doc + capture screenshots to `production/qa/evidence/map-grid-inspector-v7/`.

When complete, change evidence doc `Status:` to `COMPLETE — SIGNED OFF`. **No re-run of `/story-done` needed** — story-008 is already Complete with the manual portion explicitly tracked.

### TD-032 deferred items (informational; non-blocking)

- **A-23..A-26** (story-006 advisory tests): destroyed-wall LoS restoration, defensive guards, full 4×4 direction matrix, tie-break × non-NORTH facing (~75 min)
- **A-27..A-28** (story-007 advisory tests): warmup polish, frame-time symmetry (~10 min)
- **A-29..A-30** (perf-trend infrastructure): JSON artifact, multi-run variance check — deferred until consumer dashboard exists (~40 min)

---

## Verdict: **PASS WITH WARNINGS**

Rationale per the skill's verdict rules:

- ✅ Automated tests PASS (231/231)
- ✅ All Batch 1 / Batch 2 / Batch 3 manual checks are **N/A** for this Foundation-only epic — substituted by automated V-1..V-7 verification at the test-suite level. No FAIL signal.
- 🟡 One UI story (008) has MANUAL portion pending user sign-off in evidence doc — qualifies as PASS WITH WARNINGS per the advisory rules.

Map-grid epic is **ready for QA hand-off** (such as it is — there is no playable surface for QA to test, so QA hand-off is effectively a no-op for this Foundation-only epic; the next consumer epics will need separate QA plans).

### Next steps

1. **Manual sign-off on story-008** (~10-15 min in Godot editor)
2. **Merge PR queue** #26 → #27 → #28 → #29 (story chain) + #30 (errata, can merge in any order)
3. **`/team-qa sprint`** — formal QA cycle (in this epic's case, will be a paper review since no playable surface)
4. **`/gate-check`** — advance the project stage once `/team-qa` returns APPROVED or APPROVED WITH CONDITIONS

---

## Linked artifacts

- Sprint epic: `production/epics/map-grid/EPIC.md`
- Story files: `production/epics/map-grid/story-{001..008}-*.md`
- ADR: `docs/architecture/ADR-0004-map-grid-data-model.md`
- TR registry: `docs/architecture/tr-registry.yaml` (TR-map-grid-001..010)
- Tech debt: `docs/tech-debt-register.md` TD-032 batch (A-1..A-31; A-1..A-15 + A-16..A-22 + A-31 RESOLVED; A-23..A-30 queued)
- Story-008 manual evidence (in-progress): `production/qa/evidence/map-grid-inspector-v7.md`

## PR queue at smoke-check time

| PR | Branch | Type | Status |
|----|--------|------|--------|
| #26 | `feature/map-grid-story-005-dijkstra-movement-range` | feat | open, awaiting merge |
| #27 | `feature/map-grid-story-006-los-attack-queries` | feat | open, stacked on #26 |
| #28 | `feature/map-grid-story-007-performance-baseline` | feat | open, stacked on #27 |
| #29 | `feature/map-grid-story-008-inspector-authoring` | feat | open, stacked on #28 |
| #30 | `chore/map-grid-td-032-adr-errata` | chore (doc-only) | open, independent of #26-#29 |
