# Smoke Check Report

> **Date**: 2026-04-24
> **Scope**: Save-manager epic close-out (8 stories, 5 sprints)
> **Engine**: Godot 4.6.2.stable.official
> **Project stage**: Production (Platform-layer implementation)
> **QA Plan**: Not found — project uses epic-driven flow; save-manager EPIC.md serves as implicit coverage spec
> **Arguments**: `sprint` (interpreted as save-manager epic close-out)

---

## Automated Tests

**Status**: **PASS (162 tests, 162 passing)** — 0 errors, 0 failures, 0 flaky, 0 skipped, 0 orphans, exit 0

- Unit suite: 143 tests covering all save-manager stories 001-006 + pre-existing gamebus/scene-manager stubs
- Integration suite: 19 tests (15 pre-existing scene-manager handoff/retry/timing + 4 new save-manager perf from story-007)

Raw output summary captured at `/tmp/smoke_reg.log` during this session.

---

## CI Lint Gates

| Lint | Rule | Exit | Status |
|------|------|------|--------|
| `lint_per_frame_emit.sh` | ADR-0001 §Implementation Guidelines §7 — no per-frame GameBus emits | 0 | **PASS** |
| `lint_save_paths.sh` | ADR-0003 §Atomicity + TR-save-load-006 — save root `user://` only | 0 | **PASS** |
| `lint_enum_append_only.sh` | TR-save-load-005 — `BattleOutcome.Result` enum append-only | 0 | **PASS** |

All 3 CI lint gates green. Workflow `.github/workflows/tests.yml` runs lints BEFORE the GdUnit4 runner (fail-fast ordering preserved).

---

## Test Coverage — Save-Manager Epic (8/8 Complete)

| # | Story | Type | Test File / Evidence | Coverage |
|---|-------|------|----------------------|----------|
| 001 | SaveContext + EchoMark Resource classes | Logic | `tests/unit/core/save_context_test.gd` | COVERED |
| 002 | SaveManager autoload skeleton + project.godot registration | Logic | `tests/unit/core/save_manager_test.gd` (story-002 section) | COVERED |
| 003 | SaveManagerStub for GdUnit4 test isolation | Logic | `tests/unit/core/save_manager_stub_self_test.gd` + `save_manager_stub.gd` | COVERED |
| 004 | Save pipeline (duplicate_deep → ResourceSaver → atomic rename) | Logic | `tests/unit/core/save_manager_test.gd` (story-004 section) | COVERED |
| 005 | Load pipeline (list_slots + load_latest_checkpoint + crash-recovery scan) | Logic | `tests/unit/core/save_manager_test.gd` (story-005 section) | COVERED |
| 006 | SaveMigrationRegistry + schema version chain | Logic | `tests/unit/core/save_migration_registry_test.gd` + `save_manager_test.gd` AC-INTEGRATION | COVERED |
| 007 | Perf baseline (V-11 desktop substitute) | Integration | `tests/integration/core/save_perf_test.gd` (4 ACs; AC-TARGET DEFERRED per story §7) | COVERED (advisory — AC-TARGET Polish-phase) |
| 008 | CI lint (V-10 + V-13 + TR-005) | Config/Data | `production/qa/smoke-save-v10-v13-lint.md` (9 tests, all PASS) | MANUAL (smoke evidence) |

**Summary**: 7 COVERED, 1 MANUAL (Config/Data smoke doc), 0 MISSING.

---

## Manual Smoke Checks

**Status**: **N/A for Platform-layer epic close-out.**

Project stage is Production / Platform-layer implementation — no playable build yet, no main menu, no new-game flow, no playable scene. Standard Batch 1 (core stability — "game launches to main menu"), Batch 2 (sprint mechanic + regression), and Batch 3 (save/load data integrity + performance) all assume a playable prototype that does not exist at this stage.

**Platform-layer smoke signal is fully captured by**:
- Automated suite (unit + integration) — 162/162 PASS
- CI lint gates — 3/3 PASS
- Story-008 manual smoke doc (9/9 lint behaviors captured with verbatim outputs)
- Story-005/006/007 tests exercise save/load/migration/perf end-to-end in-process

User explicitly selected **Option A (skip manual batches)** for this close-out.

---

## Validation Criteria Coverage (ADR-0003)

| V-# | Criterion | Status | Source |
|-----|-----------|--------|--------|
| V-1 | Save cycle writes via atomic rename | **PASS** | story-004 |
| V-2 | Load returns newest checkpoint | **PASS** | story-005 |
| V-3 | SaveContext + EchoMark persist all @export fields | **PASS** | story-001 |
| V-4 | Tmp-cleanup on save failure | **PASS** | story-004 |
| V-5 | ResourceSaver error triggers tmp-cleanup | **PASS** | story-004 |
| V-6 | Schema migration chain reaches CURRENT | **PASS** | story-006 |
| V-7 | Load cycle <15 ms expected | **PASS** | story-005 implicit, story-007 measured (0.484 ms rename p95) |
| V-8 | `list_slots` returns 3 entries with correct shape | **PASS** | story-005 |
| V-9 | Corrupt file returns null + emits `save_load_failed` | **PASS** | story-005 + story-006 TD-028 #2 bonus test |
| V-10 | SAF external-storage paths rejected by lint | **PASS** | story-008 `lint_save_paths.sh` |
| V-11 | Full save cycle <50ms on mid-range Android | **DEFERRED** | Desktop substitute = 0.96 ms p95 PASS (story-007); authoritative mobile measurement pending Polish phase per TD-031 |
| V-12 | Newest-CP ordering | **PASS** | story-005 |
| V-13 | No per-frame GameBus emits from save_manager | **PASS** | story-008 `lint_per_frame_emit.sh` extension confirmed (T4 smoke test) |

**12 of 13 validation criteria fully PASS**; V-11 is explicitly DEFERRED per story-007 §7 deferral pattern (scene-manager precedent).

---

## Missing Test Evidence

**None.** All 8 save-manager stories have test/evidence coverage. AC-TARGET (V-11 authoritative) is explicitly DEFERRED per story-007 §7, not missing.

---

## Advisory Items (TD Batches)

All tracked in `docs/tech-debt-register.md`:

- **TD-028** (3 items remaining): factory helper refactor, G-16 rule-file update, negative-chapter-filename guard — low-pri, deferred
- **TD-029** (1 item deferred): A-5 migration-callable static-lint evaluation — deferred to next CI-infra polish cycle
- **TD-030** (3 items): CI lint doc nitpick (`lint_save_paths.sh`), legacy `if!` bash-bug in `lint_per_frame_emit.sh`, batch refactor proposal — deferred to next CI polish cycle
- **TD-031** (7 items): bundled with AC-TARGET Polish-phase work — rename call form fidelity, advisory threshold tightening, comment precision, `_max()` guard, payload-builder calibration vs ADR projection, AC-DESKTOP hard-bound post-baseline tightening, cosmetic checkbox

Total: **14 advisory items** — all explicitly low-priority; all explicitly scoped to future cleanup cycles; none BLOCKING.

---

## Verdict: **PASS**

### Verdict derivation (per skill rules)

**FAIL conditions — NONE apply:**
- ✓ No automated test failures (162/162 PASS)
- ✓ No Batch 1 (core stability) failures (N/A for Platform-layer)
- ✓ No Batch 2 (primary mechanic / regression) failures (N/A for Platform-layer)

**PASS WITH WARNINGS conditions — NONE apply:**
- ✓ No MISSING test evidence
- ✓ Automated tests PASS cleanly (not NOT RUN)

**PASS conditions ALL met:**
- ✓ Automated tests PASS (162/162)
- ✓ All applicable smoke checks PASS or N/A (manual batches N/A for Platform-layer)
- ✓ No MISSING test evidence
- ✓ All 3 CI lint gates PASS

V-11 AC-TARGET deferral is a documented, sanctioned deferral (not a gap); does not demote verdict per skill definitions.

---

## Next Steps

1. `/team-qa sprint` — full QA cycle (test case execution, bug triage, sign-off report). The Platform-layer scope interpretation will apply (no playable build; QA evidence is automated-suite + lints + smoke docs).
2. `/gate-check` — advance project stage once QA approves.

---

## Historical Context

This smoke check follows the epic close-out of save-manager (8/8 Complete). All 8 stories (001-008) merged to main across 5 sprints. Implementation spanned:

- Story-004: discovered 4 ADR errata (TD-024/025/026 + G-14/G-15 gotchas) — the "expensive" story that pre-paid subsequent cycles
- Stories-005/006/007: zero ADR errata (cleanest consecutive run in the project's history)
- Story-008: CI lint gates enforcing V-10/V-13/TR-save-load-005 (new infrastructure)

Session productivity on 2026-04-24: stories 006, 008, 007 closed end-to-end in a single session (each with `/dev-story` + `/code-review` + `/story-done` + commit + PR + merge cycle, 30-90 min per story).

**Save-manager epic retrospective**: ADR-0003 matured from Accepted-with-errata-risk to stable-with-zero-discovered-errata. V-1 through V-13 all addressed; V-11 authoritative validation pending on-device Polish-phase run.
